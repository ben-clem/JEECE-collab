query userById($id: Int!) {
  userById(id: $id) {
    id
    email
    firstname
    lastname
    accepted
    admin
    profilePicPath
    createdAt
    updatedAt
    service {
      id
      name
    }
    poste {
      id
      name
    }
  }
}
