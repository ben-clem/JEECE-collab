query usersByFirstnameOrLastname($name: String!) {
  usersByFirstnameOrLastname(name: $name) {
    id
    email
    firstname
    lastname
    accepted
    admin
    profilePicPath
    createdAt
    updatedAt
    serviceId
    posteId
  }
}
