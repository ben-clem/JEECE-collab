mutation Login($email: String!, $password: String!) {
  login(email: $email, password: $password) {
    errors {
      field
      message
    }
    user {
      email
      firstname
      lastname
      accepted
      admin
      profilePicPath
      createdAt
      updatedAt
      service
      poste
    }
    token
  }
}
